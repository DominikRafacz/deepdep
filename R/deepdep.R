#' Visualizing dependiences between packages 
#' 
#' @description Visualize connections between packages. 
#' 
#' @param package \code{\link{Character}} vector giving a package name.
#' @param plot_type \code{\link{Character}} string giving the type of the plot - either "circular",
#' "horizontal tree" or "vertical tree".
#' @param deps_depth \code{\link{Character}} string or \code{\link{numeric}} vector, giving 
#' information what types of dependencies to plot and how deep - see details.
#' 
#' @export
#' 
deepdep <- function(package, 
                    plot_type = "circular",
                    deps_depth = "standard") {
  NULL
}